# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# database
/prisma/dev.db
/prisma/dev.db-journal

# standalone landing-page project — merged into the main app's "/" route;
# kept on disk as a separate reference copy, not part of this repo
/Dudes-Landing-page

# standalone feasibility study doc — merged into the main app's
# /feasibility-study route; kept on disk as a reference copy
/feasibility-study

# raw esports department assets — copied into public/assets/esports;
# kept on disk as a reference copy, not part of this repo
/dudes_esport
